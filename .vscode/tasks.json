{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build jar",
            "type": "shell",
            "command": "echo Hello && cd ${workspaceFolder}/src/controlp5/ && javac -cp \"${workspaceFolder}/core.jar\" -d ./build *.java && jar cvf ${workspaceFolder}/library/controlP5.jar -C ${workspaceFolder}/src/controlP5/build .",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ]
}